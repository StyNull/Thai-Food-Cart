---
import { Image } from "astro:assets"
import ThaiCart from "@assets/images/HomePage/ThaiFoodCart.webp"
---

<section id="about-us">
    <div class="cs-container">
        <div class="cs-image-group">
            <Image 
                src={ThaiCart} 
                alt="Esan Thai food cart in Vancouver"
                width={522}
                height={461}
                loading="lazy"
                decoding="async"
                class="cs-picture"
            />
        </div>
        <div class="cs-content">
            <span class="cs-topper">Our Story</span>
            <h2 class="cs-title">Straight from Esan, Thailand</h2>
            <p class="cs-text">
                Esan is Thailand's northeastern region—known for bold, spicy cuisine that doesn't hold back. It's where our family recipes come from, passed down through generations and brought to Vancouver.
            </p>
            <p class="cs-text">
                Every dish we serve stays true to those roots. No shortcuts, no toning it down. Just real Esan food made the way it's supposed to be.
            </p>
            <!-- <a href="/menu" class="cs-button-solid">See the Menu</a> -->
        </div>
    </div>
</section>

<style>
#about-us {
    background-color: var(--backgroundColor);
    padding: clamp(3.5rem, 7vw, 7rem) 1rem;
}

#about-us .cs-container {
    width: 100%;
    max-width: 50rem;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: clamp(2rem, 5vw, 3rem);
}

#about-us .cs-image-group {
    width: 100%;
}

#about-us .cs-picture {
    width: 100%;
    height: auto;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,.08);
}

#about-us .cs-content {
    text-align: left;
    width: 100%;
    max-width: 48ch;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
}

#about-us .cs-title {
    margin: 0;
}

#about-us .cs-text {
    margin: 0;
    line-height: 1.7;
}

#about-us .cs-button-solid {
    color: #1a1a1a !important;
    margin-top: 0.5rem;
    font-size: 1rem;
    font-weight: 700;
    line-height: 1;
    text-decoration: none;
    color: var(--bodyTextColorWhite);
    background-color: var(--primary);
    padding: 1rem 2rem;
    border-radius: 14px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: background-color 0.2s, transform 0.15s;
}

#about-us .cs-button-solid:hover {
    background-color: var(--primaryLight);
    transform: scale(1.02);
}

/* Desktop */
@media (min-width: 64rem) {
    #about-us .cs-container {
        max-width: 80rem;
        flex-direction: row;
        align-items: center;
        gap: clamp(3rem, 6vw, 5rem);
    }

    #about-us .cs-image-group {
        width: 45%;
        max-width: 28rem;
        order: 2;
    }

    #about-us .cs-picture {
        aspect-ratio: 1 / 1;
    }

    #about-us .cs-content {
        width: 55%;
        max-width: none;
    }
}
</style>