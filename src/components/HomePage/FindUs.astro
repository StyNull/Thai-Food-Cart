<!-- ============================================ -->
<!--               Find Us / Hours               -->
<!-- ============================================ -->

<section id="findus-2201">
  <div class="cs-container">
    <!-- Left: Info -->
    <div class="cs-content">
      <span class="cs-topper">Find Us</span>
      <h2 class="cs-title">Visit Our Food Cart</h2>
      <p class="cs-text">
        We’re serving authentic Thai street food right in the heart of Seattle.
        Stop by for lunch or dinner — and follow us for daily specials!
      </p>

      <div class="cs-info">
        <div class="cs-block">
          <h3 class="cs-h3">Location</h3>
          <p>1234 Pike St • Seattle, WA 98101</p>
          <p class="cs-note">Across from the market – look for the yellow canopy.</p>
        </div>

        <div class="cs-block">
          <h3 class="cs-h3">Hours</h3>
          <ul class="cs-hours">
            <li><span>Mon – Thu</span><span>11 AM – 8 PM</span></li>
            <li><span>Fri – Sat</span><span>11 AM – 9 PM</span></li>
            <li><span>Sun</span><span>12 PM – 6 PM</span></li>
          </ul>
          <p class="cs-note">Closed on holidays · Sometimes sell out early 😉</p>
        </div>

        <div class="cs-buttons">
          <a href="https://maps.google.com?q=1234+Pike+St+Seattle+WA"
             target="_blank" rel="noopener" class="cs-button-solid">Get Directions</a>
          <a href="tel:+12065551234" class="cs-button-outline">Call Now</a>
        </div>
      </div>
    </div>

    <!-- Right: Map -->
    <div class="cs-map-card" aria-label="Map to food cart">
      <iframe
        class="cs-map"
        src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2689.5!2d-122.341!3d47.609!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0:0x0!2sYour%20Cart!5e0!3m2!1sen!2sus!4v0000000000"
        loading="lazy"
        referrerpolicy="no-referrer-when-downgrade"></iframe>
    </div>
  </div>
</section>

<style>

    /* Find Us / Hours */
#findus-2201 {
  background: var(--backgroundColor);           /* coconut base */
  padding: clamp(3.5rem, 7vw, 7rem) 1rem;
}

#findus-2201 .cs-container {
  max-width: 80rem;
  margin: 0 auto;
  display: grid;
  gap: clamp(2rem, 5vw, 3rem);
}

/* two-column layout on desktop */
@media (min-width: 64rem) {
  #findus-2201 .cs-container {
    grid-template-columns: 1fr 1fr;
    align-items: start;
  }
}

/* content card */
#findus-2201 .cs-content {
  background: var(--surfaceColor);              /* white card */
  border: 1px solid var(--borderColor);
  border-radius: 20px;
  box-shadow: 0 10px 30px var(--shadowColor);
  padding: clamp(1.5rem, 3vw, 2.25rem);
}

#findus-2201 .cs-title {
  margin-bottom: 0.5rem;
}

#findus-2201 .cs-text {
  margin-bottom: 1.25rem;
  line-height: 1.6;
  max-width: 48ch;
}

#findus-2201 .cs-info {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}

#findus-2201 .cs-h3 {
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--headerColor);
  margin-bottom: 0.25rem;
}

#findus-2201 .cs-hours {
  list-style: none;
  margin: 0.25rem 0 0;
  padding: 0;
  display: grid;
  gap: 0.35rem;
}
#findus-2201 .cs-hours li {
  display: flex;
  justify-content: space-between;
  font-size: 1rem;
  border-bottom: 1px dashed rgba(0,0,0,0.08);
  padding: 0.35rem 0;
}

#findus-2201 .cs-note {
  font-size: 0.95rem;
  color: var(--bodyTextColor);
  opacity: 0.85;
}

/* Buttons */
#findus-2201 .cs-buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin-top: 1rem;
}

#findus-2201 .cs-button-solid {
  background: var(--primary);
  color: var(--bodyTextColorWhite);
  padding: 0.75rem 1.75rem;
  border-radius: 14px;
  text-decoration: none;
  font-weight: 700;
  transition: background-color 0.25s, transform 0.15s;
}
#findus-2201 .cs-button-solid:hover {
  background: var(--primaryLight);
  transform: scale(1.02);
}
#findus-2201 .cs-button-outline {
  border: 2px solid var(--secondary);
  color: var(--secondary);
  background: transparent;
  padding: 0.75rem 1.75rem;
  border-radius: 14px;
  text-decoration: none;
  font-weight: 700;
  transition: border-color 0.25s, color 0.25s, transform 0.15s;
}
#findus-2201 .cs-button-outline:hover {
  border-color: var(--primary);
  color: var(--primary);
  transform: scale(1.02);
}

/* map card */
#findus-2201 .cs-map-card {
  background: var(--surfaceColor);
  border: 1px solid var(--borderColor);
  border-radius: 20px;
  box-shadow: 0 10px 30px var(--shadowColor);
  overflow: hidden;
  min-height: 18rem;
}
#findus-2201 .cs-map {
  width: 100%;
  height: clamp(18rem, 40vw, 26rem);
  border: 0;
  display: block;
}

/* spacing from previous section */
@media (max-width: 48rem) {
  #findus-2201 { padding-top: 4rem; }
}

</style>