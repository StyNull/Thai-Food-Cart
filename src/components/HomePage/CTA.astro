<!-- ============================================ -->
<!--                    CTA                       -->
<!-- ============================================ -->

<section id="cta-80">
    <div class="cs-stripes"></div>
    <div class="cs-container">
        <!-- Left Section -->
        <div class="cs-content">
            <span class="cs-topper">Visit Us</span>
            <h2 class="cs-title">Ready for Real Thai Flavor?</h2>
            <p class="cs-text">
                Fresh, wok-fired dishes made with authentic ingredients. Open today until 8 PM—come taste the difference traditional recipes make.
            </p>
            <div class="cs-button-group">
                <a class="cs-button-solid cs-primary" aria-label="view our menu" href="">View Full Menu</a>
                <a class="cs-button-solid cs-secondary" aria-label="get directions" href="">Get Directions</a>
            </div>
            <p class="cs-micro-trust">
                ⭐ 4.8 Rating • Vegetarian Options • Customizable Spice Levels
            </p>
            <p class="cs-urgency">
                We sometimes sell out early—come by soon!
            </p>
        </div>
        <!-- Right Section -->
        <picture class="cs-picture">
            <source media="(max-width: 600px)" srcset="https://csimg.nyc3.digitaloceanspaces.com/CTA/phone-m.jpg">
            <source media="(min-width: 601px)" srcset="https://csimg.nyc3.digitaloceanspaces.com/CTA/phone.jpg">
            <img aria-hidden="true" loading="lazy" decoding="async" src="https://csimg.nyc3.digitaloceanspaces.com/CTA/phone.jpg" alt="phone in hand" width="457" height="632">
        </picture>
    </div>
</section>

<style>
/*-- -------------------------- -->
<---            CTA             -->
<--- -------------------------- -*/
/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  #cta-80 {
    padding: 1.6em 2em 2em 2em;
    position: relative;
    z-index: 1;
    /* Remove background - let page background show through */
    background-color: transparent;
  }
  #cta-80:before {
    display: none;
  }
  #cta-80 .cs-stripes {
    display: none;
  }
  #cta-80 .cs-container {
    /* Rounded container with background */
    background-color: var(--secondary);
    border-radius: 2rem;
    padding: 2em;
    overflow: hidden;
    width: 100%;
    max-width: none;
    margin: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: clamp(3rem, 6vw, 4rem);
    position: relative;
    z-index: 2;
  }
  #cta-80 .cs-content {
    text-align: center;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  #cta-80 .cs-topper {
    color: var(--primaryLight);
  }
  #cta-80 .cs-title {
    color: var(--bodyTextColorWhite);
  }
  #cta-80 .cs-text {
    color: var(--bodyTextColorWhite);
    margin-bottom: 1.5rem;
    opacity: 0.95;
  }
  #cta-80 .cs-button-group {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    max-width: 20rem;
    margin-bottom: 1.5rem;
    align-items: center;
  }
  #cta-80 .cs-button-solid {
    font-size: 1rem;
    line-height: clamp(2.875em, 5.5vw, 3.5em);
    text-decoration: none;
    font-weight: 700;
    text-align: center;
    margin: 0;
    min-width: 9.375rem;
    padding: 0 2rem;
    border-radius: 14px;
    display: inline-block;
    position: relative;
    z-index: 1;
    box-sizing: border-box;
    transition: transform 0.15s, background-color 0.25s;
    width: 100%;
  }
  #cta-80 .cs-button-solid.cs-primary {
    background-color: var(--primary);
    color: var(--headerColor);
  }
  #cta-80 .cs-button-solid.cs-primary:hover {
    background-color: var(--primaryLight);
    transform: scale(1.02);
  }
  #cta-80 .cs-button-solid.cs-secondary {
    background-color: transparent;
    color: var(--bodyTextColorWhite);
    border: 2px solid var(--bodyTextColorWhite);
  }
  #cta-80 .cs-button-solid.cs-secondary:hover {
    background-color: rgba(255, 255, 255, 0.2);
    border-color: var(--bodyTextColorWhite);
    border-radius: 14px;
    transform: scale(1.02);
  }
  #cta-80 .cs-micro-trust {
    font-size: 0.875rem;
    line-height: 1.5;
    margin: 0;
    color: var(--bodyTextColorWhite);
    opacity: 0.9;
    font-weight: 500;
  }
  #cta-80 .cs-urgency {
    font-size: 0.8125rem;
    line-height: 1.5;
    margin: 0.5rem 0 0 0;
    color: var(--primaryLight);
    font-style: italic;
    opacity: 0.95;
  }
  #cta-80 .cs-picture {
    width: 100%;
    max-width: 28.5625rem;
    margin: auto;
    position: relative;
    display: block;
    order: -1;
    aspect-ratio: 300/475;
  }
  #cta-80 .cs-picture img {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    border-radius: 1.25rem 1.25rem 0 0;
    object-fit: cover;
  }
}
/* Tablet - 600px */
@media only screen and (min-width: 37.5rem) {
  #cta-80 .cs-button-group {
    flex-direction: row;
    max-width: none;
    justify-content: center;
  }
  #cta-80 .cs-button-solid {
    width: auto;
    min-width: 12rem;
  }
}
/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
  #cta-80 {
    text-align: left;
    margin-top: clamp(6.48rem, 7.128vw, 10.125rem);
    padding: 1.6em 2em 2em 2em;
    position: relative;
    z-index: 1;
    /* Keep transparent - let page background show */
    background-color: transparent;
  }
  #cta-80 .cs-container {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    position: relative;
    padding: 2em;
  }
  #cta-80 .cs-content {
    text-align: left;
    width: 50%;
    max-width: 39.375rem;
    margin: 0;
    margin-bottom: clamp(3.75rem, 6.2vw, 6.25rem);
    align-items: flex-start;
  }
  #cta-80 .cs-button-group {
    justify-content: flex-start;
  }
  #cta-80 .cs-picture {
    width: 41vw;
    max-width: 28.5625rem;
    height: clamp(145%, 44vw, 160%);
    max-height: clamp(31.25rem, 44vw, 36.125rem);
    margin: 0;
    position: absolute;
    right: 0;
    bottom: 0;
    aspect-ratio: none;
  }
  #cta-80 .cs-picture img {
    border-radius: 1.25rem 1.25rem 0 0;
  }
}
/* Small Desktop - 1024px */
@media only screen and (min-width: 64rem) {
  #cta-80 {
    margin-top: clamp(6.48rem, 9.288vw, 9rem);
  }
}
</style>